# 虚拟机自动化方案 - 项目结构

## 📁 完整目录结构

```
hjzg-autoplayer/
│
├── vm_proxy/                           # 🆕 虚拟机自动化方案
│   ├── remote_server.py               # 虚拟机代理服务器（WebSocket）
│   ├── remote_client.py               # 主机网络客户端
│   ├── remote_screen_detector.py      # 远程屏幕检测器（YOLO）
│   ├── remote_game_input.py           # 远程游戏输入（鼠标/键盘）
│   ├── remote_gui_script.py           # 主机 GUI 控制台
│   ├── start_vm_server.bat            # 虚拟机启动脚本
│   ├── start_host_client.bat          # 主机启动脚本
│   ├── config.json                   # 配置文件
│   ├── README.md                      # 详细文档
│   └── SETUP_GUIDE.md                 # 快速设置指南
│
├── scripts/                           # 自动化脚本
│   ├── base_script.py                 # 脚本基类
│   ├── dungeon_script.py              # 副本刷图脚本
│   └── example_script.py              # 示例脚本
│
├── gui_script.py                      # 原版本地 GUI
├── screen_detector.py                 # 原版本地检测器
├── game_input_advanced.py             # 原版本地输入
├── game_utils.py                      # 游戏工具函数
│
├── hjzgv1.pt                          # YOLO 模型文件
├── 启动GUI.bat                        # 原版启动脚本
│
└── README.md                          # 项目说明
```

## 🔄 数据流示意图

```
┌─────────────────────────────────────────────────────────────┐
│                       主机                          │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  RemoteGameAutomationGUI (Tkinter 界面)               │ │
│  │  - 连接管理                                             │ │
│  │  - 脚本选择                                             │ │
│  │  - 日志显示                                             │ │
│  └───────────────┬───────────────────────────────────────┘ │
│                  │                                          │
│  ┌───────────────┴───────────────────────────────────────┐ │
│  │  RemoteScreenDetector (AI 检测)                       │ │
│  │  1. 请求截图  ──────────────────────┐                 │ │
│  │  2. 接收 base64 图像  ─────────────┤                 │ │
│  │  3. 解码为 numpy 数组              │                 │ │
│  │  4. YOLO 模型检测                  │                 │ │
│  │  5. 返回检测结果                  │                 │ │
│  └──────────────────────────────────────┘                 │ │
│                  │                                          │
│  ┌───────────────┴───────────────────────────────────────┐ │
│  │  RemoteGameInput (输入控制)                            │ │
│  │  1. click(x, y)  ───────────────────┐                 │ │
│  │  2. 发送 WebSocket 消息             │                 │ │
│  │  3. 等待响应                        │                 │ │
│  │  4. 返回执行结果                    │                 │ │
│  └──────────────────────────────────────┘                 │ │
│                  │                                          │
│  ┌───────────────┴───────────────────────────────────────┐ │
│  │  SyncRemoteGameClient (网络客户端)                    │ │
│  │  - WebSocket 连接管理                                  │ │
│  │  - 消息发送/接收                                      │ │
│  │  - 同步/异步封装                                      │ │
│  └──────────────────────────────────────┘                 │
│                                                             │
└───────────────────┬───────────────────────────────────────┘
                    │
                    │ WebSocket 连接 (ws://192.168.1.100:8765)
                    │
                    │ 消息类型：
                    │  - capture → screenshot (base64 图像)
                    │  - click → response (执行结果)
                    │  - move → response (执行结果)
                    │  - key → response (执行结果)
                    │
└───────────────────┴───────────────────────────────────────┘
                    │
┌───────────────────┴───────────────────────────────────────┐
│                   虚拟机                       │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  GameProxyServer (代理服务器)                         │ │
│  │  - WebSocket 服务器                                     │ │
│  │  - 客户端管理                                           │ │
│  │  - 命令分发                                             │ │
│  └───────────────┬───────────────────────────────────────┘ │
│                  │                                          │
│  ┌───────────────┴───────────────────────────────────────┐ │
│  │  截图服务 (PIL + numpy + cv2)                        │ │
│  │  1. ImageGrab.grab() 截屏                              │ │
│  │  2. 转换为 numpy 数组                                  │ │
│  │  3. JPEG 压缩 (cv2.imencode)                           │ │
│  │  4. base64 编码                                        │ │
│  │  5. 发送回主机                                          │ │
│  └──────────────────────────────────────┘                 │ │
│                  │                                          │
│  ┌───────────────┴───────────────────────────────────────┐ │
│  │  输入服务 (pyautogui)                                 │ │
│  │  1. 接收点击指令                                       │ │
│  │  2. pyautogui.moveTo() 移动鼠标                        │ │
│  │  3. pyautogui.click() 点击                             │ │
│  │  4. 返回执行结果                                        │ │
│  └──────────────────────────────────────┘                 │ │
│                  │                                          │
│  ┌───────────────┴───────────────────────────────────────┐ │
│  │  游戏程序 (Torchlight: Infinite)                      │ │
│  │  - 游戏渲染                                            │ │
│  │  - 游戏逻辑                                            │ │
│  │  - 用户输入响应                                         │ │
│  └──────────────────────────────────────┘                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 执行流程

```
主机启动
   ↓
连接虚拟机 (WebSocket)
   ↓
用户点击"启动"
   ↓
主循环开始
   ↓
┌──────────────────────┐
│ 1. 请求截图          │ ──────→ 虚拟机截屏并返回 base64
├──────────────────────┤
│ 2. YOLO 检测对象      │ ←────── 主机接收并解码图像
├──────────────────────┤
│ 3. 计算点击位置        │
├──────────────────────┤
│ 4. 发送点击指令       │ ──────→ 虚拟机执行点击
├──────────────────────┤
│ 5. 等待响应            │ ←────── 返回执行结果
├──────────────────────┤
│ 6. 重复或结束         │
└──────────────────────┘
```

## 🔧 技术栈

### 主机端
| 组件 | 技术 | 作用 |
|------|------|------|
| GUI | Tkinter | 图形界面 |
| AI 检测 | Ultralytics YOLO | 目标检测 |
| 网络 | websockets | WebSocket 客户端 |
| 图像处理 | OpenCV, NumPy | 图像解码和处理 |

### 虚拟机端
| 组件 | 技术 | 作用 |
|------|------|------|
| 网络 | websockets | WebSocket 服务器 |
| 截图 | Pillow, OpenCV | 屏幕捕获和压缩 |
| 输入 | pyautogui | 鼠标和键盘模拟 |

## 📊 网络协议

### 客户端 → 服务器 (请求)

```json
// 截图请求
{
  "command": "capture",
  "quality": 85
}

// 点击请求
{
  "command": "click",
  "x": 960,
  "y": 540,
  "button": "left",
  "click_type": "single"
}

// 移动鼠标
{
  "command": "move",
  "x": 100,
  "y": 100
}

// 按键
{
  "command": "key",
  "key": "space",
  "duration": 0.05
}

// 心跳
{
  "command": "ping"
}
```

### 服务器 → 客户端 (响应)

```json
// 截图响应
{
  "type": "screenshot",
  "data": "base64编码的JPEG图像",
  "timestamp": 1234567890
}

// 执行结果响应
{
  "type": "response",
  "data": {
    "success": true,
    "action": "click left at (960, 540)"
  }
}

// 错误响应
{
  "type": "error",
  "message": "错误描述"
}

// 心跳响应
{
  "type": "pong"
}
```

## 🚀 部署架构

### 方案 A：单机单虚拟机
```
主机 ──┐
       │── 虚拟机 (游戏)
       │
自动化 (主机)
```

### 方案 B：主机多虚拟机
```
主机 ──┬── 虚拟机1 (游戏1)
       ├── 虚拟机2 (游戏2)
       └── 虚拟机3 (游戏3)

自动化 (主机) → 同时控制多个游戏
```

### 方案 C：远程云虚拟机
```
你的电脑 ──┐
           │── 云虚拟机 (游戏)
           │    (阿里云/腾讯云)
自动化 (你的电脑) → 控制云端游戏
```

## 📝 开发扩展

### 添加新命令（服务器端）
```python
async def handle_message(self, websocket, path):
    data = json.loads(message)
    command = data.get('command')

    if command == 'my_new_command':
        # 处理新命令
        result = await self.handle_my_command(data)
        await websocket.send(json.dumps({
            "type": "response",
            "data": result
        }))
```

### 添加新命令（客户端端）
```python
async def my_new_command(self, param1, param2):
    await self.send_message({
        "command": "my_new_command",
        "param1": param1,
        "param2": param2
    })
    response = await self.receive_message()
    return response.get("data", {})
```

---

**项目结构可视化完成！** 🦞
